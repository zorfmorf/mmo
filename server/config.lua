-- default server settings
-- DO NOT CHANGE. overwrite settings in config.lua

-- unique name for this server instance
DEFAULT_SERVER_NAME = "test.server"

-- the port to bind to
DEFAULT_GAME_PORT = 25896

-- time between server updates in seconds
NETWORK_TICK_RATE = 0.1

-- logs levels
LOG_LEVEL = LOG_INFO

-- just so that some code knows it shouldn't be run (draw)
SERVER = true
